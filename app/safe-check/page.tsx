'use client'
import { useState } from 'react'
import { Check, PhoneCall, Siren, MessageCircle, UserRound } from 'lucide-react'
import { Card, PageFooter, SahayShell, SectionLabel } from '@/components/sahay-shell'
export default function SafeCheck(){const [started,setStarted]=useState(false);return <SahayShell><div className="mb-8 max-w-2xl"><SectionLabel>Safe check · Feature 02</SectionLabel><h1 className="text-4xl font-black tracking-tight lg:text-5xl">A change in silence can be a signal.</h1><p className="mt-4 text-base leading-7 text-muted-foreground">When engagement drops, SAHAY offers a gentle, escalating path to confirm safety without waiting for a direct request for help.</p></div><div className="grid gap-6 lg:grid-cols-[1fr_0.9fr]"><Card className="border-coral/20 bg-coral/5 p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-bold text-coral">CASE MH-2026-0814</p><h2 className="mt-2 text-2xl font-extrabold">Silent distress detected</h2></div><div className="flex h-10 w-10 items-center justify-center rounded-full bg-coral/15 text-coral"><Siren size={19}/></div></div><p className="mt-4 text-sm leading-6 text-muted-foreground">SAHAY noticed a pattern of withdrawal without a direct request for help.</p><div className="mt-5 grid grid-cols-2 gap-2">{[['7 days','declining engagement'],['64%','response rate ↓'],['340%','response delay ↑'],['5','check-ins missed']].map(([v,l])=><div key={l} className="rounded-xl border border-coral/10 bg-background/70 p-4"><p className="text-2xl font-black text-coral">{v}</p><p className="mt-1 text-[10px] font-semibold text-muted-foreground">{l}</p></div>)}</div><button onClick={()=>setStarted(true)} className="mt-6 flex w-full items-center justify-center gap-2 rounded-full bg-coral py-3 text-sm font-bold text-primary-foreground">{started?<Check size={16}/>:<PhoneCall size={16}/>} {started?'Safe check initiated':'Initiate safe check'}</button></Card><Card className="p-6"><SectionLabel>Safe-check protocol</SectionLabel><h2 className="text-2xl font-extrabold">A gentle escalation path.</h2><ol className="mt-6 space-y-4">{[['Re-attempt preferred-language IVRS',PhoneCall],['Send WhatsApp or SMS follow-up',MessageCircle],['Offer immediate human support',UserRound],['Escalate to designated officer',Siren]].map(([label,Icon],i)=>{const I=Icon as typeof PhoneCall;return <li key={label as string} className="flex items-center gap-3"><span className="flex h-7 w-7 items-center justify-center rounded-full bg-primary text-xs font-bold text-primary-foreground">{i+1}</span><div className="flex-1"><p className="text-sm font-bold">{label as string}</p><p className="text-xs text-muted-foreground">{i===0?'Try the person’s preferred channel first.':'Only continue when the previous step receives no response.'}</p></div><I size={16} className="text-primary"/></li>})}</ol><div className="mt-7 rounded-xl bg-secondary p-4 text-xs leading-5 text-muted-foreground">No action is taken without a clear audit trail and an assigned human owner.</div></Card></div><PageFooter/></SahayShell>}
